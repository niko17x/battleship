// get index of key 'x' in object.
// use index pos to get the item in the alpha array.
// increment the alpha using the length of the ship.

const xInBound = ["a", "b", "c", "d", "e", "f", "g", "h", "i", "j"];
const obj = [{ x: "b", y: 2 }]; // Use map method for object array.
const obj1 = { x: "b", y: 3 }; // Use dot notation (obj1.x) for just objects.
